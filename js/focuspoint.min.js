!function(t, i) {
    "use strict";
    i.IFA = {
        Models: {},
        Collections: {},
        Views: {},
        css: {
            _imageFocus: "image-focus",
            imageFocus: {
                _wrapper: "image-focus__wrapper",
                _img: "image-focus__img",
                _point: "image-focus__point",
                _clickarea: "image-focus__clickarea",
                _button: "image-focus__button"
            },
            _button: "button",
            button: {
                _primary: "button-primary",
                _disabled: "button-disabled"
            }
        },
        CalcService: {
            minMaxRange: function(t, i, e) {
                return Math.min(e, Math.max(i, t));
            }
        }
    }, t(document).on("ready", function() {
        new IFA.Views.ImageFocus({});
    });
}(jQuery, window), function(t, i) {
    "use strict";
    IFA.Models.Attachment = Backbone.Model.extend({
        url: ajaxurl,
        apiArgs: [ "action" ],
        id: !1,
        defaults: {
            src: !1,
            width: !1,
            height: !1,
            offset: {
                x: !1,
                y: !1
            },
            focusPointOrigin: {
                x: !1,
                y: !1
            },
            focusPoint: {
                x: 50,
                y: 50
            },
            differState: !1,
            ajaxState: !1
        },
        $img: !1,
        initialize: function(t) {
            var i = this;
            this.$img = t.$img, this.fetchAttachmentData(), this.on("change:focusPointOrigin", function() {
                i.setDifferState(!0);
            }, this), this.on("change:differState", this.resetAjaxState, this);
        },
        fetchAttachmentData: function() {
            var i = this;
            this.fetch({
                url: ajaxurl + "?action=get-focuspoint",
                data: t.param({
                    id: this.id
                }),
                success: function(t, e, s) {
                    if (!0 === e.success) try {
                        i.validateFocusPoint(e), i.set({
                            src: e.src
                        }), i.set({
                            focusPointOrigin: e.focusPoint
                        }), i.set({
                            focusPoint: e.focusPoint
                        });
                    } catch (t) {
                        console.log(t);
                    }
                },
                error: function(t, i, e) {
                    console.log(i);
                }
            });
        },
        setDifferState: function(t) {
            var i = this, e = this.get("focusPointOrigin"), s = this.get("focusPoint"), o = !1;
            e.x !== s.x && (o = !0), e.y !== s.y && (o = !0), this.set("differState", o), !0 !== t && !1 !== o || this.once("change:focusPoint", function() {
                i.setDifferState(!1);
            }, this);
        },
        resetAjaxState: function() {
            !0 === this.differState && self.model.set("ajaxState", !1);
        },
        validateFocusPoint: function(t) {
            if (!t.focusPoint.hasOwnProperty("x") || !t.focusPoint.hasOwnProperty("y")) throw "Wrong object properties";
        }
    });
}(jQuery, window), function(t, i) {
    IFA.Models.FocusInterface = Backbone.Model.extend({
        defaults: {
            width: 0,
            height: 0,
            radius: 0,
            offset: {
                x: 0,
                y: 0
            },
            position: {
                x: 0,
                y: 0
            },
            clickPosition: {
                x: 0,
                y: 0
            },
            state: {
                init: !1,
                move: !1,
                active: !1,
                hover: !1
            }
        },
        initialize: function() {},
        setState: function(t) {
            var i = this.get("state") || {};
            _.extend(i, t), this.set({
                state: i
            });
        }
    });
}(jQuery, window), function(t, i) {
    "use strict";
    IFA.Views.Attachment = Backbone.View.extend({
        initialize: function() {
            t(i).on("resize", _.bind(this.updateDimensions, this)), this.model.once("change:focusPoint", this.updateDimensions, this);
        },
        updateDimensions: function() {
            var t = this.model.$img, i = {
                x: t.offset().left,
                y: t.offset().top
            };
            this.model.set({
                width: t.width(),
                height: t.height(),
                offset: i
            });
        }
    });
}(jQuery, window), function(t, i) {
    "use strict";
    IFA.Views.Cropbutton = Backbone.View.extend({
        defaults: {
            ajaxState: !1
        },
        $cropButton: !1,
        initialize: function(i, e) {
            var s = this, o = '<button type="button" class="' + IFA.css._button + " " + IFA.css.button._disabled + " crop-attachment " + IFA.css.imageFocus._button + '">' + focusPointL10n.cropButton + "</button>";
            this.$el.append(o), this.$cropButton = t("." + IFA.css.imageFocus._button), this.$cropButton.on("click", function() {
                s.model.set("ajaxState", "cropping"), s.model.save({}, {
                    url: ajaxurl + "?action=initialize-crop",
                    success: function(t, i, e) {
                        if (!0 === i.success) try {
                            s.model.validateFocusPoint(i), s.model.set({
                                focusPointOrigin: i.focusPoint
                            }), s.model.set("ajaxState", "success");
                        } catch (t) {
                            console.log(t), s.model.set("ajaxState", "failed");
                        }
                    },
                    error: function(t, i, e) {
                        s.model.set("ajaxState", "failed"), console.log(i);
                    }
                });
            }), this.model.on("change:differState", this.differStateHandler, this), this.model.on("change:ajaxState", this.ajaxStateHandler, this);
        },
        differStateHandler: function() {
            !0 === this.model.get("differState") ? this.highlight() : this.disable();
        },
        ajaxStateHandler: function() {
            switch (this.model.get("ajaxState")) {
              case "cropping":
                this.setButtonText(focusPointL10n.cropButtonProgress), this.disable();
                break;

              case "success":
                this.setButtonText(focusPointL10n.cropButtonSuccess), this.disable();
                break;

              case "failed":
                this.setButtonText(focusPointL10n.cropButtonFailed), this.activate();
                break;

              default:
                this.setButtonText(focusPointL10n.cropButton);
            }
        },
        setButtonText: function(t) {
            this.$cropButton.text(t);
        },
        highlight: function() {
            this.$cropButton.removeClass(IFA.css.button._disabled).addClass(IFA.css.button._primary).text(focusPointL10n.cropButton);
        },
        activate: function() {
            this.$cropButton.removeClass(IFA.css.button._disabled).removeClass(IFA.css.button._primary);
        },
        disable: function() {
            this.$cropButton.removeClass(IFA.css.button._primary).addClass(IFA.css.button._disabled);
        }
    });
}(jQuery, window), function(t, i) {
    "use strict";
    IFA.Views.FocusInterface = Backbone.View.extend({
        template: !1,
        $img: !1,
        imgHtml: !1,
        $imageFocus: !1,
        $container: !1,
        $clickarea: !1,
        $focusPoint: !1,
        initialize: function(t, i) {
            this.attachment = t.attachment, this.template = wp.template("IFA-focus-point"), 
            this.$img = i.$img, this.$img.addClass(IFA.css.imageFocus._img), this.imgHtml = this.$img.get(0).outerHTML, 
            this.$img.wrap('<div class="IFA-container"></div>'), this.$container = this.$el.find(".IFA-container"), 
            this.render(), this.model.on("change:position", this.updateFocusPoint, this), this.attachment.once("change:focusPoint", this.updateDimensionData, this), 
            this.attachment.once("change:focusPoint", this.updateFocusPoint, this);
        },
        events: {},
        render: function() {
            var t = this.attachment.get("focusPoint");
            this.$container.html(this.template({
                imageObject: this.imgHtml,
                left: t.x,
                top: t.y,
                state: "is-initialized"
            })), this.setElements(), this.setEvents();
        },
        setElements: function() {
            this.$imageFocus = this.$container.find("." + IFA.css._imageFocus), this.$clickarea = this.$container.find("." + IFA.css.imageFocus._clickarea), 
            this.$focusPoint = this.$container.find("." + IFA.css.imageFocus._point), this.attachment.$img = this.$img = this.$container.find("." + IFA.css.imageFocus._img), 
            this.attachment.$img = this.$img;
        },
        setEvents: function() {
            var e = this;
            this.$clickarea.on("mousedown", function(t) {
                1 === t.which && e.startMove(t, !0).move(t);
            }), this.$focusPoint.on("mousedown", function(t) {
                1 === t.which && e.startMove(t);
            }).on("mouseenter", function() {
                e.toggleHoverState(!0);
            }).on("mouseleave", function() {
                e.toggleHoverState(!1);
            }), t(i).on("mouseup", function(t) {
                1 === t.which && (e.model.setState({
                    move: !1,
                    active: !1
                }), e.$imageFocus.removeClass("is-active"));
            }).on("mousemove", function(t) {
                e.move(t);
            }).on("resize", function() {
                e.updateDimensionData().updateFocusPoint();
            });
        },
        startMove: function(t, i) {
            return this.updateDimensionData().updateClickPosition(t, i), this.$imageFocus.addClass("is-active"), 
            this.model.setState({
                move: !0,
                active: !0
            }), this;
        },
        move: function(t) {
            if (!1 === this.model.get("state").move) return !1;
            var i = {
                x: t.pageX,
                y: t.pageY
            }, e = {}, s = this.attachment.get("offset"), o = this.model.get("clickPosition"), n = this.attachment.get("width"), a = this.attachment.get("height");
            e.x = i.x - s.x - o.x, e.y = i.y - s.y - o.y, e.x = IFA.CalcService.minMaxRange(e.x, 0, n), 
            e.y = IFA.CalcService.minMaxRange(e.y, 0, a);
            var c = {};
            return c.x = e.x / n * 100, c.y = e.y / a * 100, this.attachment.set({
                focusPoint: c
            }), this.model.set({
                position: e
            }), this;
        },
        updateFocusPoint: function() {
            var t = this.attachment.get("focusPoint"), i = this.model.get("position"), e = this.model.get("radius"), s = this.attachment.get("width"), o = this.attachment.get("height"), n = this.attachment.get("src"), a = {};
            return a.x = 0 - (i.x - e), a.y = 0 - (i.y - e), this.$focusPoint.css({
                left: t.x + "%",
                top: t.y + "%",
                backgroundImage: 'url("' + n + '")',
                backgroundSize: s + "px " + o + "px ",
                backgroundPosition: a.x + "px " + a.y + "px "
            }), this;
        },
        updateClickPosition: function(t, i) {
            var e = {
                x: 0,
                y: 0
            };
            if (!0 !== i) {
                var s = {
                    x: t.pageX,
                    y: t.pageY
                }, o = this.model.get("offset");
                (e = {}).x = s.x - o.x, e.y = s.y - o.y;
            }
            return this.model.set({
                clickPosition: e
            }), this;
        },
        updateDimensionData: function() {
            this.model.width = this.$focusPoint.width(), this.model.height = this.$focusPoint.height();
            var t = this.model.width / 2;
            this.model.set("radius", t);
            var i = this.$focusPoint.offset();
            this.model.set({
                offset: {
                    x: i.left + t,
                    y: i.top + t
                }
            });
            var e = this.attachment.get("focusPoint"), s = {
                x: e.x / 100 * this.attachment.get("width"),
                y: e.y / 100 * this.attachment.get("height")
            };
            return this.model.set({
                position: s
            }), this;
        },
        toggleHoverState: function(t) {
            this.model.setState({
                hover: t
            }), this.$imageFocus.toggleClass("is-hover", t);
        }
    });
}(jQuery, window), function(t, i) {
    "use strict";
    IFA.Views.ImageFocus = Backbone.View.extend({
        initialize: function() {
            var i = this;
            setInterval(function() {
                var e = t(".attachment-details");
                e.length && !t(".IFA-initialized").length && e.each(function() {
                    var e = t(this);
                    i.startApplication(e);
                });
            }, 500);
        },
        startApplication: function(t) {
            var i = t.find(".details-image");
            t.addClass("IFA-initialized");
            var e = new IFA.Models.Attachment({
                id: t.data("id"),
                $img: i
            }), s = (new IFA.Views.Attachment({
                model: e
            }), new IFA.Models.FocusInterface({}));
            new IFA.Views.FocusInterface({
                el: t,
                model: s,
                attachment: e
            }, {
                $img: i
            }), new IFA.Views.Cropbutton({
                el: t.find(".attachment-actions"),
                model: e
            });
            s.set("state.init", !0);
        }
    });
}(jQuery, window);
//# sourceMappingURL=focuspoint.min.js.map