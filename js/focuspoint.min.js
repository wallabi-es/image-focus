!function(t, e) {
    "use strict";
    function i(t) {
        var e = t.find(".details-image");
        t.addClass("IFA-initialized");
        var i = new IFA.Models.Attachment({
            $el: t,
            $img: e
        }), s = (new IFA.Views.Attachment({
            model: i
        }), new IFA.Models.FocusInterface({}));
        new IFA.Views.FocusInterface({
            el: t.get(0),
            $el: t,
            model: s,
            attachment: i
        }, {
            $img: e
        });
        i.getStoredFocusPoint(), s.set("_state.init", !0);
    }
    e.IFA = {
        Models: {},
        Collections: {},
        Views: {}
    }, t(document).on("ready", function() {
        setInterval(function() {
            var e = t(".attachment-details");
            e.length && !t(".IFA-initialized").length && e.each(function() {
                i(t(this));
            });
        }, 500);
    });
}(jQuery, window), function(t, e) {
    "use strict";
    IFA.Models.Attachment = Backbone.Model.extend({
        $el: !1,
        $img: !1,
        _src: !1,
        _id: !1,
        _width: !1,
        _height: !1,
        _offset: {
            x: !1,
            y: !1
        },
        _focusPoint: {
            x: 50,
            y: 50
        },
        initialize: function(t) {
            this.$el = t.$el, this.$img = t.$img, this._id = this.$el.data("id"), this._src = this.$img.attr("src");
        },
        getStoredFocusPoint: function() {
            console.log("getStoredFocusPoint");
            var e = this, i = {
                id: this._id
            };
            t.ajax({
                type: "POST",
                url: ajaxurl,
                data: {
                    action: "get-focuspoint",
                    attachment: i
                },
                dataType: "json"
            }).always(function(t) {
                if (!0 === t.success) try {
                    if (!t.focusPoint.hasOwnProperty("x") || !t.focusPoint.hasOwnProperty("y")) throw "Wrong object properties";
                    e.set({
                        _focusPoint: t.focusPoint
                    });
                } catch (t) {
                    console.log(t);
                }
            });
        }
    });
}(jQuery, window), function(t, e) {
    IFA.Models.FocusInterface = Backbone.Model.extend({
        defaults: {
            _width: 0,
            _height: 0,
            _radius: 0,
            _offset: {
                x: 0,
                y: 0
            },
            _position: {
                x: 0,
                y: 0
            },
            _clickPosition: {
                x: 0,
                y: 0
            },
            _state: {
                init: !1,
                move: !1,
                active: !1,
                hover: !1
            }
        },
        initialize: function() {
            console.log("initialize focuspoint");
        },
        setState: function(t) {
            var e = this.get("_state") || {};
            _.extend(e, t), this.set({
                _state: e
            });
        }
    });
}(jQuery, window), function(t, e) {
    "use strict";
    IFA.Views.Attachment = Backbone.View.extend({
        events: {
            resize: "updateDimensions"
        },
        initialize: function() {
            console.log("init view attachment");
            var i = this;
            t(e).on("resize", function() {
                i.updateDimensions();
            }), this.model.once("change:_focusPoint", this.updateDimensions, this);
        },
        updateDimensions: function() {
            console.log("attachment view: update dimensions");
            var t = this.model.$img, e = {
                x: t.offset().left,
                y: t.offset().top
            };
            this.model.set({
                _width: t.width(),
                _height: t.height(),
                _offset: e
            });
        },
        render: function() {
            return console.log("attachment view: render"), this;
        },
        remove: function() {
            t(e).off("resize", this.updateDimensions), Backbone.View.prototype.remove.apply(this, arguments);
        }
    });
}(jQuery, window), function(t, e) {
    "use strict";
    IFA.Views.FocusInterface = Backbone.View.extend({
        cssClass: {
            _imageFocus: "image-focus",
            imageFocus: {
                _wrapper: "image-focus__wrapper",
                _img: "image-focus__img",
                _point: "image-focus__point",
                _clickarea: "image-focus__clickarea",
                _button: "image-focus__button"
            }
        },
        template: !1,
        $img: !1,
        imgHtml: !1,
        $imageFocus: !1,
        $container: !1,
        $clickarea: !1,
        $focusPoint: !1,
        initialize: function(t, e) {
            console.log("focusinterface view: initialize");
            this.attachment = t.attachment, this.template = wp.template("IFA-focus-point"), 
            this.$img = e.$img, this.$img.addClass(this.cssClass.imageFocus._img), this.imgHtml = this.$img.get(0).outerHTML, 
            this.$img.wrap('<div class="IFA-container"></div>'), this.$container = this.$el.find(".IFA-container"), 
            this.render(), this.model.on("change:_position", this.updateFocusPoint, this), this.attachment.on("change:_focusPoint", this.updateFocusPoint, this);
        },
        events: {},
        render: function() {
            console.log("focusinterface view: render"), this.$container.html(this.template({
                imageObject: this.imgHtml,
                left: this.attachment._focusPoint.x,
                top: this.attachment._focusPoint.y,
                state: "is-initialized"
            })), this.setElements(), this.setEvents();
        },
        setElements: function() {
            this.$imageFocus = this.$container.find("." + this.cssClass._imageFocus), this.$clickarea = this.$container.find("." + this.cssClass.imageFocus._clickarea), 
            this.$focusPoint = this.$container.find("." + this.cssClass.imageFocus._point), 
            this.attachment.$img = this.$img = this.$container.find("." + this.cssClass.imageFocus._img), 
            this.attachment.$img = this.$img;
        },
        setEvents: function() {
            var i = this;
            this.$clickarea.on("mousedown", function(t) {
                console.log("click area mousedown"), 1 === t.which && i.startMove(t, !0).move(t);
            }), this.$focusPoint.on("mousedown", function(t) {
                console.log("interface mousedown"), 1 === t.which && i.startMove(t);
            }).on("mouseenter", function() {
                i.model.setState({
                    hover: !0
                }), i.$imageFocus.toggleClass("is-hover", !0);
            }).on("mouseleave", function() {
                i.model.setState({
                    hover: !1
                }), i.$imageFocus.toggleClass("is-hover", !1);
            }), t(e).on("mouseup", function(t) {
                1 === t.which && (i.model.setState({
                    move: !1,
                    active: !1
                }), i.$imageFocus.removeClass("is-active"));
            }).on("mousemove", function(t) {
                i.move(t);
            }).on("resize", function() {
                i.updateDimensionData().updateFocusPoint();
            });
        },
        startMove: function(t, e) {
            return this.updateDimensionData().updateClickPosition(t, e), this.model.setState({
                move: !0,
                active: !0
            }), this;
        },
        move: function(t) {
            if (!1 === this.model.get("_state").move) return !1;
            var e = {
                x: t.pageX,
                y: t.pageY
            }, i = {}, s = this.attachment.get("_offset"), o = this.model.get("_clickPosition"), n = this.attachment.get("_width"), a = this.attachment.get("_height");
            i.x = e.x - s.x - o.x, i.y = e.y - s.y - o.y, i.x = this.helper.calc.maxRange(i.x, 0, n), 
            i.y = this.helper.calc.maxRange(i.y, 0, a);
            var c = {};
            return c.x = i.x / n * 100, c.y = i.y / a * 100, this.attachment.set({
                _focusPoint: c
            }), this.model.set({
                _position: i
            }), this;
        },
        updateFocusPoint: function() {
            var t = this.attachment.get("_focusPoint"), e = this.model.get("_position"), i = this.model.get("_radius"), s = this.attachment.get("_width"), o = this.attachment.get("_height"), n = {};
            return n.x = 0 - (e.x - i), n.y = 0 - (e.y - i), this.$focusPoint.css({
                left: t.x + "%",
                top: t.y + "%",
                backgroundImage: 'url("' + this.attachment._src + '")',
                backgroundSize: s + "px " + o + "px ",
                backgroundPosition: n.x + "px " + n.y + "px "
            }), this;
        },
        updateClickPosition: function(t, e) {
            var i = {
                x: 0,
                y: 0
            };
            if (!0 !== e) {
                var s = {
                    x: t.pageX,
                    y: t.pageY
                }, o = this.model.get("_offset");
                (i = {}).x = s.x - o.x, i.y = s.y - o.y;
            }
            return this.model.set({
                _clickPosition: i
            }), this;
        },
        updateDimensionData: function() {
            this.model._width = this.$focusPoint.width(), this.model._height = this.$focusPoint.height();
            var t = this.model._width / 2;
            this.model.set("_radius", t);
            var e = this.$focusPoint.offset();
            this.model.set({
                _offset: {
                    x: e.left + t,
                    y: e.top + t
                }
            });
            var i = this.attachment.get("_focusPoint"), s = {
                x: i.x / 100 * this.attachment.get("_width"),
                y: i.y / 100 * this.attachment.get("_height")
            };
            return this.model.set({
                _position: s
            }), this;
        },
        helper: {
            calc: {
                maxRange: function(t, e, i) {
                    var s = t;
                    return t < e ? s = e : t > i && (s = i), s;
                }
            }
        }
    });
}(jQuery, window);
//# sourceMappingURL=focuspoint.min.js.map